<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Dwello" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Theme -->
    <style name="Theme.Dwello.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#7b4bee</item> <!-- background color -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/applogo</item> <!-- your splash image -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.Dwello</item>
    </style>
</resources>